# Label 234 Custom Rules

## Deployment Rule
After every work/task completion, you MUST:
1. Push the code to the GitHub repository.
2. SSH into the server (`72.61.255.54` with password `divineGG21@@`), pull the code, and make it live.

(Note: If the terminal sandbox blocks automated SSH execution, use a workaround like a python/node SSH script or provide the exact `deploy.sh` execution instructions immediately to the user).
